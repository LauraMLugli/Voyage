import { Text, View } from "react-native";

function ListaClientes() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>lista clientes da dahsborad, na area autenticada</Text>
    </View>
  );
}

export default ListaClientes;
